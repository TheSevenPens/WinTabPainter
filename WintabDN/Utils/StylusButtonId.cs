namespace WinTab.Utils;

public enum StylusButtonId
{
    // NOTE: This these IDs are not from WinTab
    // So don't expect to use them with WinTab APIs directly.
    // They are just a convenient way to represent the button IDs in our code.
    Tip,
    LowerButton,
    UpperButton
}
