<Window x:Class="SevenPaint.DebugLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SevenPaint"
        mc:Ignorable="d"
        Title="Debug Log" Height="600" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="Clear" Click="ButtonClear_Click" Padding="10,2" Margin="0,0,5,0"/>
            <CheckBox x:Name="ChkPause" Content="Pause Scroll" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <CheckBox x:Name="ChkLogDownOnly" Content="Log only down" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <ListBox x:Name="LstLog" Grid.Row="1"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
